module.exports = function(RED) {
    function word2vecNode(config) {
        RED.nodes.createNode(this,config);
        var node = this;
    }
    // RED.nodes.registerType("word2vec",word2vecNode);
}

